{
  "accum_iter": 1,
  "betas": null,
  "consumed_samples_per_epoch": 10560,
  "effective_batch_size": 32,
  "epochs": 45,
  "epsilon": 1e-08,
  "final_norm": false,
  "last_mct": true,
  "last_mct_configuration": {
    "classifier": "shared Conv2d(D, 20, kernel_size=1) / F.linear",
    "enabled": true,
    "eps": 1e-06,
    "fft_dimension": "embedding",
    "patch_final_norm": true,
    "score_formula": "P / abs(P_lowpass - P).clamp_min(eps)",
    "selection_semantics": "independent patch index per embedding channel",
    "sigma": 19.595917942265423,
    "topk": 1,
    "topk_dimension": "patch"
  },
  "micro_batch_size": 32,
  "minimum_lr": 1e-05,
  "nominal_lr": 0.0005,
  "optimizer": "adamw",
  "optimizer_lr": 3.125e-05,
  "optimizer_updates_per_epoch": 330,
  "patch_final_norm": true,
  "schedule": "cosine",
  "seed": 0,
  "train_dataset_size": 10582,
  "val_batch_size": 32,
  "warmup_epochs": 5,
  "weight_decay": 0.05,
  "world_size": 1
}
